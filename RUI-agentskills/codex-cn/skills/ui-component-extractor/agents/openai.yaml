interface:
  display_name: "UI Component Extractor"
  short_description: "自动识别并执行组件抽离重构"
  default_prompt: "Use $ui-component-extractor to split oversized components and improve reuse."

policy:
  allow_implicit_invocation: true
