interface:
  display_name: "Requirements Elicitation Engine"
  short_description: "结构化反问并生成可执行PRD草案"
  default_prompt: "使用 $requirements-elicitation-engine 澄清需求并生成 PRD 草案产物。"

policy:
  allow_implicit_invocation: true
