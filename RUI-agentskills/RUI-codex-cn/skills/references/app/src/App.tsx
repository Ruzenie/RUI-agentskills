import React from 'react';

/**
 * Workspace reference snapshot for skill linking.
 * This file is not used as runtime source code in the current repository.
 */
export default function App(): JSX.Element {
  return (
    <main>
      <h1>App Workspace Snapshot</h1>
      <p>Used by skills as an in-package reference path.</p>
    </main>
  );
}
