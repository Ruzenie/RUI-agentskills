import React from 'react';

/**
 * Workspace reference snapshot for skill links.
 * Not used as runtime application code in this repository.
 */
export default function App(): JSX.Element {
  return (
    <main>
      <h1>App Workspace Snapshot</h1>
      <p>In-package reference for skill documentation.</p>
    </main>
  );
}
