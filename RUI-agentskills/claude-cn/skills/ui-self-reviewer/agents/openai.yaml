interface:
  display_name: "UI Self Reviewer"
  short_description: "执行代码/视觉/交互/审美自审"
  default_prompt: "Use $ui-self-reviewer to run a structured self-review before shipping."

policy:
  allow_implicit_invocation: true
